# ── Web framework (config dashboard API) ──────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-dotenv>=1.0.0
requests>=2.32.0

# ── Discord bot ───────────────────────────────────────────────────
discord.py>=2.4.0

# ── LangChain + Gemini (LangChain 1.x API) ───────────────────────
langchain>=1.0.0
langchain-core>=0.3.0
langchain-google-genai>=2.0.0
langchain-community>=0.3.0

# ── Redis + MongoDB (memory stores) ────────────────────────────
redis>=5.0.0
pymongo>=4.7.0

# ── Kafka (for voice→text bridge) ─────────────────────────────
kafka-python>=2.0.0

# ── Testing ───────────────────────────────────────────────────────
pytest>=8.0.0

# ── Token counting for memory/context budgeting ──────────────────
tiktoken>=0.7.0
