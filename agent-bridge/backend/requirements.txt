# ── Web framework (config dashboard API) ──────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-dotenv>=1.0.0
requests>=2.32.0

# ── Discord bot ───────────────────────────────────────────────────
discord.py>=2.4.0

# ── LangChain + Gemini (LangChain 1.x API) ───────────────────────
langchain>=1.0.0
langchain-core>=0.3.0
langchain-google-genai>=2.0.0
langchain-community>=0.3.0

# ── Redis + MongoDB (memory stores) ────────────────────────────
redis>=5.0.0
pymongo>=4.7.0

# ── Kafka (config-events consumer for multi-org Discord connection
#    discovery — see core/config_events.py, core/tool_config_consumer.py).
#    Same library python-consumer/requirements.txt already declares for the
#    sibling process; core/config_events.py lives in the main backend
#    package, not python-consumer, so it needs its own declaration here too.
kafka-python>=2.0.0

# ── Testing ───────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.24.0

# ── Token counting for memory/context budgeting ──────────────────
tiktoken>=0.7.0
